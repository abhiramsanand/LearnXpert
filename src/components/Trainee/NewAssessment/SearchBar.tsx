import React from 'react';
import { TextField } from '@mui/material';

const SearchBar: React.FC = () => {
  return (
    <TextField label="Search Assessments" variant="outlined" fullWidth />
  );
};

export default SearchBar;
