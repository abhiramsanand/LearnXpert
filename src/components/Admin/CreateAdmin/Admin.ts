// src/types/Admin.ts
export interface Admin {
    id: number;      // ID of the admin
    userName: string;    // Name of the admin
    username: string; // Username of the admin
    email: string;   // Email of the admin
  }
  